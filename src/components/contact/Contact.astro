<section class="contact">
  <span class="badge">Contacto</span>
  <h2>Contáctenos</h2>
  <p>Nos encantaría saber de usted. Complete este formulario.</p>

  <form>
    <label for="name">
      <span>Nombre <span class="required">*</span></span>
      <input type="text" name="name" placeholder="Nombre" />
    </label>

    <label for="lastname">
      <span>Apellidos <span class="required">*</span></span>
      <input type="text" name="lastname" placeholder="Apellidos" />
    </label>

    <label for="email">
      <span>Correo electrónico <span class="required">*</span></span>
      <input type="text" name="email" placeholder="tu@correo.com" />
    </label>
    <label for="phone">
      <span>Número de celular</span>
      <input type="text" name="phone" placeholder="+51 999 999 999" />
    </label>

    <label for="message">
      <span>Mensaje <span class="required">*</span></span>
      <textarea name="message" rows="4"></textarea>
    </label>

    <div class="politicas">
      <input class="input-check" type="checkbox" name="check" />
      <span>Acepta nuestra <a href="/">política de privacidad</a>.</span>
    </div>

    <button role="button">Enviar mensaje</button>
  </form>
</section>

<style>
  .contact {
    padding: 32px 16px;
    text-align: center;
    background: hsl(210deg 20% 98.04%);
  }

  .badge {
    display: inline-flex;
    color: var(--primary-color);
    font-size: 14px;
    font-weight: 500;
    padding: 4px 12px;
    background-color: rgb(0 152 181 / 10%);
    border-radius: 20px;
    margin-bottom: 8px;
  }

  h2 {
    letter-spacing: -0.9px;
    font-weight: 700;
    font-size: 24px;
    margin-top: 8px;
  }
  p {
    margin-top: 16px;
    font-size: 16px;
    color: var(--text-description);
    line-height: 24px;
    margin-bottom: 32px;
  }

  form {
    display: flex;
    flex-direction: column;
    gap: 26px;
  }

  label {
    display: flex;
    flex-direction: column;
    gap: 8px;
    text-align: start;
    font-size: 14px;
    font-weight: 500;
    line-height: 20px;
  }

  input,
  textarea {
    width: 100%;
    font-size: 14px;
    font-weight: 500;
    border-radius: 8px;
    background: #fff;
    padding: 12px;
    line-height: 20px;
    border: 1px solid var(--border-color);
    color: var(--text-description);
    outline: none;
  }

  .required {
    color: red;
    font-size: 14px;
  }

  .politicas {
    display: inline-flex;
    gap: 8px;
    color: var(--text-description);
    font-size: 14px;
    font-weight: 400;
    line-height: 23px;
    align-items: center;
  }

  .input-check {
    width: 16px;
    height: 16px;
  }

  a {
    color: var(--primary-color);
    font-weight: 500;
  }

  button {
    background: var(--primary-color);
    color: #fff;
    border: none;
    border-radius: 8px;
    height: 44px;
    font-weight: 500;
    font-size: 14px;
    cursor: pointer;
    transition: background 0.2s ease-in-out;
  }

  button:hover {
    background: #007a91;
  }
</style>
